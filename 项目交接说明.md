# Flight Flow 项目交接说明

这个项目是一个给 iPad 使用的飞行阶段提醒 PWA。它面向 A320 运行 / 模拟机训练场景，用于按飞行阶段显示易忘项目、风险提示、环境条件触发提示和模拟机科目提示。

正式地址：

https://tianming-798.github.io/flight-flow/

GitHub 仓库：

https://github.com/tianming-798/flight-flow

本地项目路径通常是：

```powershell
C:\Users\13506\Documents\运行进程
```

## 当前状态

- 当前应用版本：0.3.6
- 更新时间：2026-07-20 20:30
- 部署方式：推送 `main` 分支后由 GitHub Actions 自动部署到 GitHub Pages
- 数据存储：每台设备自己的浏览器 IndexedDB，本项目当前没有云同步
- 离线能力：PWA + Service Worker，iPad 首次联网打开并缓存后可离线使用

## 技术栈

- React
- TypeScript
- Vite
- vite-plugin-pwa / Service Worker
- IndexedDB / Dexie
- Vitest
- GitHub Pages / GitHub Actions 自动部署

项目不需要服务器。正式版是可安装、可离线运行的 PWA。

## 核心文件

- `src/App.tsx`
  - 主界面
  - 当前阶段 / 总览切换
  - 左侧阶段预览逻辑
  - 环境条件弹窗
  - 飞机信息弹窗
  - 模拟机科目中心
  - 版本更新记录弹窗
  - 设置、导入导出、阶段推进、遗漏确认

- `src/data.ts`
  - 21 个飞行阶段
  - 默认环境规则
  - 默认模拟机科目模板
  - 字段名称、操作符名称
  - 应用版本号、更新时间、版本更新记录 `changelog`
  - F1 跟班流程静态数据 `f1GuidePhases`

- `src/rules.ts`
  - 环境规则匹配
  - 侧风 / 顶风计算
  - 正常流程、环境规则、模拟机科目的提示聚合
  - 去重与来源合并

- `src/types.ts`
  - TypeScript 数据类型
  - 环境数据结构
  - 规则、科目、阶段提示、飞行进程结构

- `src/db.ts`
  - IndexedDB 初始化
  - 老用户打开新版时自动补齐新增默认规则

- `src/styles.css`
  - 全局样式
  - iPad 横屏优先布局
  - 风险 / 易忘分区
  - 颜色分级、触控尺寸
  - 飞机信息、版本更新记录、阶段预览样式

- `src/rules.test.ts`
  - 规则匹配测试
  - 侧风计算测试
  - 提示去重测试

- `.github/workflows/deploy.yml`
  - GitHub Pages 自动部署流程

## 21 个飞行阶段

顺序不能随便改，除非明确知道会影响已有勾选状态和阶段记录。

1. 外部安全检查
2. 驾驶舱初始准备
3. 外部绕机检查
4. 驾驶舱准备
5. 推出或起动前
6. 发动机起动
7. 起动后
8. 滑行
9. 起飞前
10. 起飞
11. 起飞后
12. 爬升
13. 巡航
14. 下降准备
15. 下降
16. 进近
17. 着陆
18. 复飞
19. 着陆后
20. 停机
21. 安全离机

## 当前已实现的重点功能

### 阶段进程

- 当前阶段模式
- 总览模式
- 上一阶段 / 下一阶段
- 已完成阶段显示
- 存在未勾选易忘项目时，进入下一阶段会弹出遗漏确认
- 左侧阶段列表和总览卡片只用于查看 / 预览，不会提前标记阶段完成
- 查看非当前执行阶段时，底部显示“回到当前阶段”
- 查看非当前执行阶段时，易忘项目不能被误勾选
- 只有底部“完成并进入下一阶段”才会推进当前阶段和完成状态

### 环境条件

环境条件和飞机信息已经拆成两个独立入口。

“环境条件”只包含天气和跑道相关字段：

- 机场
- 气温
- 风向
- 风速
- 阵风
- 能见度
- 是否降水
- 是否需要除 / 防冰
- 跑道
- 跑道方向
- 跑道代码
- 刹车效应
- 备注

所有字段都是选填。空字段不参与规则判断，不报错，不阻止阶段推进。

运行方式按气温自动判断：

- 未填写气温：未判定
- 气温 > 30℃：高温运行
- 气温 ≤ 0℃：冬季运行
- 其他：常规运行

如果“高温运行”等环境规则已经命中，顶部不会再重复显示同名运行方式标签。

### 飞机信息

“飞机信息”只包含飞机和重量相关字段：

- 机型
- 油量，单位吨
- 0 燃油重量，单位吨

飞机信息页会显示预计起飞重量：

```text
预计起飞重量 = 油量 + 0 燃油重量
```

当前这个数值只是显示预览，后续可用于大重量起飞风险规则。


### F1 跟班流程

顶部工具栏新增“F1跟班”入口。这个流程独立于 21 个正式运行阶段，适合 F1 阶段从网上准备到航后讲评使用。

阶段包括：

1. 网上准备
2. 准备室
3. 机组车去机场
4. 上飞机
5. 飞行中
6. 下飞机
7. 航后讲评

内容来源于用户提供的跟班攻略截图，已去重整理成风险提示和易忘项目。数据在 `src/data.ts` 的 `f1GuidePhases` 中维护，界面在 `src/App.tsx` 的 `F1GuideModal` 中渲染。

F1 跟班顶部有“航班信息”区域，保存到 `FlightSession.f1FlightInfo`：

- 起飞机场：天府 / 双流
- 航线类型：默认国内，可切换国际
- 日期：可选，`date`
- 起飞时刻：`time`，用于 iPad 上更稳定地录入时间
- 原始航班信息：`rawFlightText`，用于粘贴排班/航班详情文本并自动解析

准备时间规则：

- 天府国内：起飞前 110 分钟开始准备
- 天府国际：起飞前 130 分钟开始准备
- 双流：起飞前 100 分钟开始准备
- 天府起飞时间在 12:00 以前：网上准备阶段自动出现“前一天 21:00 前完成签到”提醒

单独的“复制给机长的时间信息”已删除；当前只保留完整的给机长/二哥微信模板复制功能。

F1 跟班还内置时间计算器，可计算两个时长相加，输入框默认空白，支持“1小时20分钟”“1:20”“1.20”“80”等格式。其中“1.20”表示 1 小时 20 分钟。

F1 跟班还支持粘贴航班信息生成微信模板。解析逻辑在 `src/App.tsx` 的 `F1GuideModal` 内：

- 从 `航班号[FLT]` 提取航班号
- 从 `机号[ARN]` 提取机号
- 从 `起飞时间[STD]` 提取日期和起飞时刻
- 从 `航段[SEGMENTS]` 提取航线，例如 `TFU-YNT` 转为 `天府-烟台`
- 从机组人员行提取中文姓名：第一个为机长，第二个为二哥，最后一个为本人
- 根据机号后缀在 `aircraftTailTypeMap` 中识别具体机型，不使用 `机型[ACT]` 代码。例如 `B-326A` → `A320-251N`，`B-32NJ` → `A319-153N`
- `aircraftTailTypeMap` 已录入用户提供图片中的机号-机型映射；后续新增机号时优先维护这张表

生成模板中需要用户人工确认/补充的字段统一用 `——`，包括飞行经历时间、保留/OEB 和第几次参与航班运行。

F1 跟班流程的勾选状态复用 `FlightSession.checked`，key 以 `f1:` 开头，例如：

```text
f1:online-prep:online-message
```

新增或修改 F1 流程时，优先改 `f1GuidePhases`，并同步更新 `changelog`。

### 已内置的环境规则

规则在 `src/data.ts` 的 `defaultRules` 中维护。

- 高温运行
  - 条件：气温 > 30℃
  - 驾驶舱准备：易忘项目“黄+蓝超压泵放襟翼 1”

- 冬季运行（模板）
  - 默认停用
  - 条件模板：气温 ≤ 0℃

- 降水运行
  - 条件：是否降水 = 有降水
  - 驾驶舱初始准备：排风扇超控
  - 驾驶舱准备到滑行：雨刷相关风险提示
  - 滑行：转弯速度低于 7 kt
  - 起飞前：进跑道前雨刷 FAST
  - 起飞后：关雨刷、排风扇解除超控
  - 进近：五边雨刷 FAST
  - 着陆后：雨刷 SLOW
  - 停机：进机位关雨刷

- 跑道代码 444
  - 条件：跑道代码 = 444
  - 起飞：关键风险“跑道代码 444：油门 TOGA”

- 低能见度
  - 条件：能见度 ≤ 400m
  - 驾驶舱准备：易忘项目“LS 按钮开”

- 低能见度 + 侧风限制
  - 条件：能见度 ≤ 400m 且侧风 > 15 kt
  - 起飞前 / 起飞：关键风险“低能见度且侧风大于 15 kt：不能起飞”

- 需要除 / 防冰
  - 条件：需要除 / 防冰 = 需要
  - 起动后：延迟放襟翼直到进跑道
  - 滑行：执行卸冰程序
  - 着陆后：延迟收襟翼直到机位上

### 模拟机科目中心

- 支持新建、编辑、复制、删除科目
- 支持按 21 个阶段维护科目内容
- 支持中文、英文缩写、关键词、别名和拼音首字母搜索
- 支持同时激活多个科目
- 支持标记科目已处置

默认科目模板：

- 双 FMGC 失效
- 地面 LGCIU 1 失效

### 版本更新记录

设置齿轮里有“版本更新记录”。

版本记录由 `src/data.ts` 的 `changelog` 维护。发布新版本时，应同步更新：

- `package.json` 的 `version`
- `package-lock.json` 的 `version`
- `src/data.ts` 的 `APP_VERSION`
- `src/data.ts` 的 `APP_UPDATED_AT`
- `src/data.ts` 的 `changelog`
- `src/data.ts` 的 `f1GuidePhases`（如更新 F1 跟班流程）
- 如有外部交接需要，更新 `项目交接说明.md`

## 数据保存与同步逻辑

用户数据保存在当前浏览器的 IndexedDB 中。

主要数据：

- 当前飞行进程
- 当前阶段
- 已勾选项目
- 环境条件
- 飞机信息
- 激活的模拟机科目
- 用户维护的规则
- 用户维护的科目库
- 正常流程模板

重要：

- iPad 和电脑的数据不会自动同步。
- iPad 上添加的项目只存在 iPad 浏览器里。
- 电脑网站上添加的项目只存在电脑浏览器里。
- 更新网站代码通常不会删除 iPad 上已经保存的数据。
- 新增默认规则时，应通过 `src/db.ts` 的 bootstrap 逻辑补齐，而不是只在空数据库时写入。
- 如果修改数据结构，要考虑旧数据兼容或迁移。
- 不要轻易更改规则和项目的 `id`，否则可能影响历史勾选状态恢复。

### 手动同步方式

当前同步方式是 JSON 完整备份。

推荐流程：

1. 在内容最新的设备上打开网站。
2. 设置齿轮 → 导出完整备份。
3. 得到 `.json` 文件。
4. 通过 AirDrop、微信文件传输、iCloud、邮箱等方式发到另一台设备。
5. 另一台设备打开网站。
6. 设置齿轮 → 导入备份。
7. 选择合并或覆盖。

建议把 iPad 当作飞行使用主库，并定期导出 JSON 备份。

## 修改规则的建议方式

### 新增一个简单环境规则

通常改 `src/data.ts` 的 `defaultRules`。

一个规则包含：

- `id`：稳定唯一标识，不要随便改
- `name`：用户看到的来源名称
- `enabled`：是否默认启用
- `groups`：触发条件
- `outputs`：命中后注入到哪些阶段
- `updatedAt`

条件逻辑：

- 同一个 group 内全部满足，相当于“并且”
- 多个 group 之间任一满足，相当于“或者”

常用字段：

- `temperature`
- `visibility`
- `windDirection`
- `windSpeed`
- `runwayHeading`
- `crosswind`
- `headwind`
- `precipitation`
- `antiIceRequired`
- `runwayCode`
- `fuelTons`
- `zeroFuelWeightTons`

### 新增环境输入字段

需要同时检查这些位置：

1. `src/types.ts`
   - 给 `EnvironmentData` 增加字段

2. `src/data.ts`
   - 给 `fieldLabels` 增加中文名称

3. `src/App.tsx`
   - 环境字段放在 `EnvironmentModal`
   - 飞机 / 重量字段放在 `AircraftModal`
   - 如果是数字字段，加入 `numericFields`

4. `src/rules.test.ts`
   - 如果字段参与规则判断，最好补测试

### 新增计算字段

例如未来的大重量起飞：

- 可以在 `src/rules.ts` 的 `fieldValue` 中支持派生字段
- 或者先在 `EnvironmentData` 中显式保存

当前已有派生字段：

- `crosswind`
- `headwind`

它们通过风向、风速、跑道方向计算。

## 开发与验证命令

进入项目目录：

```powershell
cd "C:\Users\13506\Documents\运行进程"
```

安装依赖：

```powershell
npm install
```

本地运行：

```powershell
npm run dev
```

本地浏览器访问：

```text
http://localhost:5173/
```

同一个 Wi-Fi 下，iPad 可以访问电脑局域网地址，例如：

```text
http://192.168.x.x:5173/
```

注意：局域网地址只是开发预览，不是正式安装版。

运行测试：

```powershell
npm test -- --run
```

生产打包：

```powershell
npm run build
```

## 部署流程

本项目已经配置 GitHub Actions。

只要把 `main` 分支推送到 GitHub，GitHub Pages 会自动部署。

```powershell
git status
git add .
git commit -m "说明这次改了什么"
git push origin main
```

推送后等待 GitHub Actions 完成，然后访问：

https://tianming-798.github.io/flight-flow/

如果 iPad 已经安装到主屏幕：

1. 让 iPad 联网。
2. 打开主屏幕里的 Flight Flow。
3. 如未更新，完全退出再打开一次。
4. 再断网测试是否可用。

## 给后续 AI 的注意事项

如果你是后续接手修改的 AI，请先读这些文件：

1. `README.md`
2. `项目交接说明.md`
3. `src/types.ts`
4. `src/data.ts`
5. `src/rules.ts`
6. `src/App.tsx`
7. `src/db.ts`
8. `src/rules.test.ts`

请遵守：

- 不要删除 21 个阶段。
- 不要随意改变已有阶段 `id`。
- 不要随意改变已有规则和项目 `id`。
- 不要把选填字段改成必填。
- 不要加入服务器依赖，除非用户明确要求云同步。
- 不要破坏离线 PWA 能力。
- 不要清空用户 IndexedDB 数据。
- 不要让左侧阶段预览触发完成状态。
- 修改后必须运行 `npm test -- --run` 和 `npm run build`。
- 如果新增默认规则，要确保老用户打开新版后也能自动补齐。
- 如果新增版本，应同步维护版本更新记录。

## 常见需求示例

### 示例：新增大重量起飞风险

需求可能是：

> 根据飞机信息中的油量和 0 燃油重量计算起飞重量，超过某阈值时，在起飞前或起飞阶段加入风险提示。

推荐做法：

1. 在 `src/rules.ts` 中增加派生字段，例如 `takeoffWeightTons`。
2. 在 `src/types.ts` 的 `EnvironmentField` 允许这个字段。
3. 在 `src/data.ts` 的 `fieldLabels` 增加“起飞重量”。
4. 在 `defaultRules` 中新增规则。
5. 在 `src/rules.test.ts` 中补阈值测试。
6. 在 `src/data.ts` 的 `changelog` 中追加版本记录。

### 示例：新增一个模拟机科目模板

改 `src/data.ts` 的 `defaultSubjects`。

注意：

- 新科目的 `id` 要稳定唯一。
- 如果希望老用户也自动获得新增默认科目，可能需要扩展 `src/db.ts`，像默认规则一样补齐缺失科目。

### 示例：接入云同步

当前没有云同步。如果用户明确要求，可考虑两级方案：

- 轻量方案：GitHub Gist / 云端 JSON，手动上传和下载。
- 完整方案：Supabase / Firebase 登录与数据库同步。

实现前需要明确冲突处理策略，不要默认自动覆盖用户本地数据。

## 当前版本

- 应用版本：0.3.6
- 更新时间：2026-07-20 20:30

版本号和更新记录在这些地方维护：

- `package.json`
- `package-lock.json`
- `src/data.ts` 的 `APP_VERSION`
- `src/data.ts` 的 `APP_UPDATED_AT`
- `src/data.ts` 的 `changelog`
- `src/data.ts` 的 `f1GuidePhases`（如更新 F1 跟班流程）
- `项目交接说明.md`

如果发布新功能，请同步更新它们。
